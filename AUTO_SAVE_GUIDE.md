# 💾 نظام الحفظ التلقائي
# Auto-Save System

## 🎯 الهدف
حفظ جميع التغييرات تلقائياً في Git مع كل تعديل بدون الحاجة للتدخل اليدوي.

---

## 📋 الطرق المتاحة

### 1️⃣ طريقة سهلة وسريعة: **Auto-Save**
- **الملف:** `auto-save.ps1`
- **الوصف:** يتحقق من التغييرات كل 5 ثوانٍ ويحفظها فوراً
- **التشغيل:**
  ```powershell
  .\auto-save.ps1
  ```
- **من VS Code:** اضغط `Ctrl+Shift+B` واختر "💾 Auto-Save (Watcher)"

### 2️⃣ طريقة متقدمة: **Auto-Commit**
- **الملف:** `auto-commit.ps1`
- **الوصف:** مراقبة متقدمة مع استثناء ملفات معينة
- **التشغيل:**
  ```powershell
  .\auto-commit.ps1
  ```
- **من VS Code:** اضغط `Ctrl+Shift+B` واختر "💾 Auto-Commit (Advanced Watcher)"

---

## ⚙️ الإعدادات

### تعديل الفترة الزمنية (للطريقة السهلة):
```powershell
$checkInterval = 5000  # 5 ثوانٍ - غير هذا الرقم
```

### استثناء ملفات معينة (للطريقة المتقدمة):
انظر داخل `auto-commit.ps1`:
```powershell
$excludePatterns = @(
    '\.git',
    '\.vscode',
    'node_modules',
    'dist',
    'build',
    # أضف أنماط أخرى هنا
)
```

---

## 🚀 خطوات البدء السريعة

### من داخل VS Code:
1. اضغط `Ctrl+Shift+P`
2. اكتب "Tasks: Run Task"
3. اختر "💾 Auto-Save (Watcher)"
4. ستظهر رسالة: "🔄 سكريبت الحفظ التلقائي قيد التشغيل..."

### من PowerShell:
```powershell
cd "C:\Users\Reem\Desktop\بوت"
.\auto-save.ps1
```

---

## 📊 ماذا يحدث؟

```
✏️ تعدل ملف
    ↓
🔍 البرنامج يكتشف التغيير
    ↓
💾 بعد 5 ثوانٍ: git add -A
    ↓
📝 git commit مع وقت التعديل
    ↓
🚀 git push تلقائياً
    ↓
✅ الملف محفوظ على GitHub
```

---

## ⏹️ إيقاف البرنامج

اضغط `Ctrl+C` في terminal حيث يعمل السكريبت

---

## 🔔 الرسائل التي قد تراها

| الرسالة | المعنى |
|--------|--------|
| 📝 تم اكتشاف تغييرات | وجدت تعديلات جديدة |
| ✅ تم الحفظ | تم حفظ الملفات محلياً |
| 🚀 تم الرفع | تم رفع التغييرات للـ GitHub |
| ✅ لا توجد تغييرات جديدة | لا شيء جديد للحفظ |

---

## ⚠️ ملاحظات مهمة

1. **لا تاثير على الأداء:** البرنامج خفيف جداً ولا يؤثر على عمل البوت
2. **الملفات المستثناة:** `.git`, `node_modules`, `.vscode` وغيرها لا تُحفظ
3. **رسائل الـ Commit:** تحتوي على الوقت تلقائياً مثل "⏰ Auto-save: 14:30:45"
4. **الإنترنت:** تأكد من وجود اتصال إنترنت أثناء العمل

---

## 💡 تلميحات

- استخدم **Auto-Save** للحفظ البسيط والسريع
- استخدم **Auto-Commit** إذا كنت تريد تحكم أكثر دقة
- يمكنك تشغيل السكريبت والبوت معاً في نفس الوقت
- الملفات الموقتة (.env, .ps1) لا يتم حفظها تلقائياً

---

## 🆘 حل المشاكل

**المشكلة:** رسالة "cannot be loaded because running scripts is disabled"

**الحل:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

**المشكلة:** لا توجد رسائل في Terminal

**الحل:** تأكد من أنك في المجلد الصحيح وملفات التخزين موجودة

---

**تم الإعداد بنجاح! 🎉**
